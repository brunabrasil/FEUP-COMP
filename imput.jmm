class Test {

    boolean a;
    boolean b;

  public boolean foo() {
        boolean result;
         if(!a && !b){
                  result = a;
                } else {
        			result = b;
                }

        return true;
  }
}